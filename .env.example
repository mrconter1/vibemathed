# Copy to .env and fill in. See README "Local development" for how to get each
# value. Never commit .env - only this example file is tracked.
# (Prisma CLI reads .env, not .env.local, so everything lives in .env.)

# CockroachDB connection string. The current setup shares the stone-grivet
# serverless cluster with wilhelm-scream-db, but uses its own `vibemathed`
# database within it.
DATABASE_URL="postgresql://user:password@host:26257/vibemathed?sslmode=verify-full"

# Signing secret for Auth.js sessions. Generate one with:
#   npx auth secret
AUTH_SECRET=""

# Google OAuth client, from Google Cloud Console > APIs & Services >
# Credentials > OAuth client ID (type: Web application).
# Authorized redirect URIs must include:
#   http://localhost:3000/api/auth/callback/google   (local)
#   https://<your-domain>/api/auth/callback/google   (production)
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

# GitHub OAuth app, from GitHub > Settings > Developer settings > OAuth Apps >
# New OAuth App. One app per environment, because an OAuth App accepts only a
# single callback URL:
#   http://localhost:3000/api/auth/callback/github   (local)
#   https://<your-domain>/api/auth/callback/github   (production)
# Homepage URL can be the site root. No scopes need requesting; the default
# gives the public profile and primary email, which is all that is read.
AUTH_GITHUB_ID=""
AUTH_GITHUB_SECRET=""
